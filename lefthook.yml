pre-commit:
  jobs:
    - run: pnpm exec ultracite fix {staged_files}
      glob:
        - "**/*.js"
        - "**/*.jsx"
        - "**/*.ts"
        - "**/*.tsx"
        - "**/*.json"
        - "**/*.jsonc"
        - "**/*.css"
      stage_fixed: true
    - run: pnpm knip
    - name: rust-format
      glob:
        - "src-tauri/*.rs"
        - "src-tauri/**/*.rs"
        - "crates/**/*.rs"
        - "rustfmt.toml"
        - ".rustfmt.toml"
        - "src-tauri/rustfmt.toml"
        - "src-tauri/.rustfmt.toml"
        - "rust-toolchain.toml"
      run: cargo fmt --all -- --check
