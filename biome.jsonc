{
  "$schema": "./node_modules/@biomejs/biome/configuration_schema.json",
  "extends": [
    "ultracite/biome/core",
    "ultracite/biome/react",
    "ultracite/biome/tanstack",
    "ultracite/biome/vitest"
  ],
  "files": {
    // `src/typeset.css` is vendored byte for byte (D40) and `assets/` holds the
    // icon sources for `scripts/icons.sh`, which never reach a DOM. Specta's
    // bindings stay byte for byte so drift checks compare the generator's output.
    "includes": [
      "!!src/typeset.css",
      "!!assets",
      "!!src/server/adapters/bindings.ts"
    ]
  },
  "overrides": [
    {
      // Regenerated by `scripts/update-shadcn.sh`, so non-autofixable lint is
      // turned off rather than patched into the next regeneration (D19, D37).
      "includes": ["src/components/ui/**"],
      "linter": {
        "rules": {
          "a11y": {
            "noLabelWithoutControl": "off",
            "noNoninteractiveElementInteractions": "off",
            "useKeyWithClickEvents": "off",
            "useSemanticElements": "off"
          },
          "performance": {
            "noJsxPropsBind": "off",
            "noNamespaceImport": "off"
          },
          "style": {
            "useDestructuring": "off"
          },
          "suspicious": {
            "noLeakedRender": "off"
          }
        }
      }
    }
  ]
}
