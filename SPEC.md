# SPEC: notras → local-first Tauri desktop app

> Working document for the rewrite. Update checkboxes and the progress log as work lands.

## Context

notras is a single-user, local-first notes app, wrapped in Next.js 16 and shipped as a
Docker image before this rewrite. The web-app framing had failed. The replacement is a
desktop app that takes "just write, otra vez" seriously and makes AI a first-class
co-writer of notes.

The pivotal decision, inspired by
[erictli/scratch](https://github.com/erictli/scratch): notes are plain `.md` files you own
rather than database rows. That one choice is what makes the AI story free. Claude Code or
any agent writes markdown into the notes folder, a file watcher picks it up, the app
refreshes. The filesystem is the API, so there is no MCP server, no endpoints, and nothing
to build or run.

SQLite stays, demoted. The same FTS5 machinery that powers search today becomes a derived,
disposable index owned by Rust and rebuilt from the files at any time.

## Decisions

Moved to [DECISIONS.md](DECISIONS.md), where each entry carries its rationale and what it
rejected. The set this spec locked, plus the choices the rewrite settled along the way,
runs `D1` through `D31`.

## Feature set (final)

### Keep and build

- markdown notes as files; filename is the title (inline title field renames the file)
- TipTap 3 WYSIWYG editor over `@tiptap/markdown`: the rendered document rather than
  styled source, covering headings, bold and italic, bullets, clickable task checkboxes
  (writing `[x]` back to the file), editable tables, inline images via the asset
  protocol, lowlight-highlighted code blocks, horizontal rules
- the editor holds the note body; frontmatter is parsed off at load and
  reattached at save from the latest loader snapshot
- autosave on ~800ms idle, flushed on blur, note switch, and quit
- ⌘P raw source mode: the whole file (frontmatter included) in one
  lowlight-highlighted code block, with the caret carried across the toggle
- FTS5 search (bm25, snippets) via the ⌘K palette
- tags (frontmatter `tags:`), pins (frontmatter `pinned:`), folders (real directories)
- wikilinks `[[note title]]` with autocomplete, replacing the old links sidebar with
  links between the user's own notes; rendered as clickable pills in the editor
- slash commands: `/` quick-insert menu (heading, task list, code block, date)
- open external `.md`: drag-and-drop or "Open With" any markdown file
- code-block chrome (copy, language picker); link editing (⌘⇧K) and
  ⌘-click to open in the browser
- drag a file/image into the editor (or paste an image) → copied to `attachments/`,
  markdown link inserted; images render via the Tauri asset protocol, whose scope
  follows the notes dir at runtime
- iA-Writer-style writing niceties: focus mode (dim all but the active paragraph),
  typewriter scrolling (active line stays centered), word count + reading time in
  the status strip, Literata as the editor face (SIL OFL, bundled; `D24`)
- global quick-capture: system-wide hotkey → small always-on-top window → jot → esc
- menu-bar tray icon (open / new note / quick capture), launch-at-login toggle
- macOS overlay title bar (hidden title, traffic lights over content)
- keyboard shortcuts everywhere; lowercase aesthetic; dark/light via system

### Cut

Each of these removes machinery, not only a screen. `D17` carries the table.

- ~~link extraction + OG previews~~ → og/link services, `links` table, only network dep
- ~~reminders~~ → SSE route, 30s polling fiber, notification plugin, reminder presets
- ~~export/import zip~~ → the folder is the export; fflate, export/import services
- ~~image optimization~~ → sharp/Rust image pipeline; attachments are copied as-is
- ~~profile/greeting~~ → user table, user-service, device-user seeding, profile form
- ~~asset BLOBs~~ → blob IPC, `/api/assets`, uploader/preview components
- ~~cache invalidation machinery~~ → `CacheInvalidator` port, `forkBackground` (no
  remaining consumers); mutations await, then `router.invalidate()`

## Architecture

Moved to [ARCHITECTURE.md](ARCHITECTURE.md), which holds the diagram, the single-writer
rule, the layer boundaries, the key patterns, the index schema, and the invariants.

### What survives from the current codebase

| Survives                                                                                | Dies                                                                     |
| --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `src/core/**` minus reminder-presets, cache-invalidator, background (knip will confirm) | `src/app/**`, `src/actions/**` (rewritten as `src/data/`)                |
| `fts-query.ts`, FTS semantics, `[[hl]]` snippet markers + `fts-snippet.ts` renderer     | link/og/asset/export/import/user services + repos                        |
| Effect service pattern, `makeAppLayer`, `ManagedRuntime`                                | `CacheInvalidator` + `Fetcher` need, `forkBackground`                    |
| note/folder/tag services (rewired to FileStore + Index)                                 | drizzle-kit, `@libsql/client`, sharp, fflate usage                       |
| shadcn/ui components, lowercase aesthetic, hotkeys (the stone palette went in `D23`)    | user table, device-user, safe-action, nuqs                               |
|                                                                                         | next-safe-action, next-themes, @t3-oss/env-nextjs, zod                   |
|                                                                                         | SSE reminders, NoteDropPanel, SiteNav, home hero page                    |
|                                                                                         | `format-service` + remark-parse/stringify (`D15`)                        |
|                                                                                         | preview stack: react-markdown, remark-gfm, rehype-expressive-code (`D7`) |

## Phase 0: scaffold

- [x] `src-tauri/` (tauri 2.11.x); plugins: `dialog`, `opener`, `store`,
      `global-shortcut`, `autostart` (no `http`, `notification`, `sql`, or even `fs`,
      since all note IO goes through Rust commands so the dynamic notes-dir scope is
      enforced at runtime)
- [x] Cargo: `rusqlite` (bundled, so FTS5 is compiled in unconditionally), `notify`,
      `serde`, `serde_json`, `serde_yaml`
- [x] `vite.config.ts`: react, `@tailwindcss/vite`, `@tanstack/router-plugin`, alias `@`,
      port 1420 strictPort
- [x] `tauri.conf.json`: hidden title bar + overlay traffic lights, `.md` file
      association, tray icon, second window config for quick-capture
- [x] Scripts: `dev` → `tauri dev`, `build` → `tauri build`, `dev:web`, `build:web`

## Phase 1: Rust core (files + index)

- [x] `notes_dir` resolution: from store config, default `~/notras` (created on first run)
- [x] `ensure_index_schema` on startup at `<notes_dir>/.notras/index.db`
- [x] Indexer: scan + mtime reconcile on startup; write commands index
      synchronously (no separate index step); frontmatter parse; FTS upsert;
      delete handling
- [x] `notify` watcher (debounced ~300ms, ignores `.notras/`) → reindex → emit
      `notes-changed { paths }` to webview
- [x] `db_select(sql, params)` command (rejects anything that isn't
      SELECT/WITH, since Rust is the only writer)
- [x] Commands: `rename_note(from, to)`, `set_notes_dir(path)` (re-scan + re-watch)

## Phase 2: TS data layer

- [x] `src/core/frontmatter.ts`: parse/serialize `{ pinned, tags }` + body (tests; parity
      with Rust)
- [x] `FileStore` port (Context.Service in core) + `src/server/adapters/tauri-file-store.ts`
      (Rust commands, scoped to notes dir)
- [x] `Index` access: Drizzle `sqlite-proxy` over `db_select` in
      `src/server/adapters/tauri-database.ts`; new lean schema defs
- [x] Rewrite `note-repository`: reads = SELECTs against index (list/search/filters port
      over); writes = FileStore only, since the Rust command that writes the file also
      writes the index in the same call and TypeScript never writes the index (`D4`)
- [x] folder/tag repositories folded into `note-repository` reads (folders
      emerge from paths; tags from frontmatter)
- [x] Preferences (`notesDir`) persisted by Rust through the store and read from
      TypeScript through the `FileStore` port, never a direct plugin-store import;
      autostart plugin
- [x] `runtime.ts`: `ManagedRuntime.make(makeAppLayer({ fileStore, database }))`
- [x] `src/actions/` → `src/data/`: plain async fns; validation stays Effect Schema;
      mutations end with `router.invalidate()`

## Phase 3: editor

- [x] TipTap 3 + `@tiptap/markdown` (bidirectional GFM); StarterKit, tables, task
      lists, images, lowlight code blocks; theme on the stone/oklch tokens, dark via
      system. Editing is body-only: frontmatter is parsed off at load and recomposed
      at save from the latest loader snapshot
- [x] Markdown round-trip contract (`markdown-roundtrip.spec.ts`): every node must
      define its markdown form or externally-authored files lose content
- [x] Autosave: 800ms idle debounce → save file → index; serialized on one chain so
      overlapping flushes cannot land out of order; flushed on blur, unmount, and quit
      (Rust holds `ExitRequested` until the webview reports back)
- [x] Focus mode (⌘D): dim non-active paragraphs; typewriter scrolling toggle
      (scroll active line to vertical center)
- [x] Status strip: saved state · word count · reading time
- [x] Bundle Literata (+ iA Writer Mono for code spans) as the editor font (`D24`)
- [x] Wikilink autocomplete: `[[` triggers completion sourced from index titles;
      custom tokenizer + node so pills survive the markdown round trip
- [x] Slash commands: `/` → completion menu (headings, lists, quote, code block,
      table, divider, date), matched on label and shorthand hint
- [x] ⌘P raw source mode: the whole file in one lowlight-highlighted code block; the
      caret rides across the toggle as a sentinel through the markdown converters
- [x] Code-block chrome (copy, language picker); link editing (⌘⇧K) with url
      normalization and a scheme allowlist; ⌘-click opens in the browser
- [x] Inline title field above editor → `rename_note`
- [x] Drag file/image in (or paste an image) → copy to `attachments/`, insert
      markdown link

## Phase 4: shell and UI

- [x] Routes: `__root.tsx`, `index.tsx` (redirect to last note or empty state),
      `notes.$.tsx` (splat for nested dirs), `external.tsx` (files outside the
      notes dir)
- [x] ⌘K palette (shadcn Command/cmdk): FTS search with highlighted snippets, filter
      tokens (tag/folder/pinned), recent notes, "create note", "settings"
- [x] Empty state: wordmark + "just write, otra vez." + ⌘n / ⌘k hints
- [x] Settings dialog (⌘,): notes folder picker (plugin-dialog), launch at login
- [x] `notes-changed` listener → reload clean buffer / refresh palette + lists
- [x] Hotkeys: ⌘n new, ⌘k palette, ⌘p source mode, ⌘d focus, ⌘, settings,
      ⌘⇧k link (shift+/ shortcuts help: still todo)
- [x] Delete/pin/tag via palette actions + editor toolbar strip
- [x] Port lowercase aesthetic, globals.css tokens, Toaster; delete SiteNav, home hero,
      NoteDropPanel, dnd-kit (drag-to-folder replaced by palette "move to folder")

## Phase 5: desktop niceties

- [x] Quick capture: global shortcut → small always-on-top window, bare editor →
      `inbox/<timestamp>.md`, esc saves & hides
- [x] Tray: open, new note, quick capture, quit; close-to-tray behavior
- [x] Autostart plugin wired to settings toggle
- [x] "Open With" / drag-onto-window for external `.md` (outside notes dir: editable,
      not indexed)

## Phase 6: teardown

- [x] Delete: `src/app/**`, old actions, safe-action, nuqs params, env.ts, next/postcss
      configs, Dockerfile, PWA/icons, SSE route, all cut services/repos/components,
      `drizzle.config.ts`, e2e/ (Playwright can't drive Tauri; wdio+tauri-driver is a
      follow-up), nextjs-agent-rules block
- [x] Drop deps: next, next-safe-action, next-themes, @t3-oss/env-nextjs, zod, nuqs,
      sharp, fflate, @libsql/client, drizzle-kit, @dnd-kit/*, react-hook-form,
      @hookform/resolvers, @playwright/test (use-debounce stays for the palette and
      external-file autosave)
- [x] Add: @tauri-apps/api + 3 plugins (dialog, opener, autostart),
      @tanstack/react-router, the `@tiptap/*` set including `@tiptap/markdown` as the
      canonical serializer (`D7`, `D15`), lowlight, cmdk (via shadcn); dev:
      @tauri-apps/cli, @tanstack/router-plugin, @tailwindcss/vite
- [x] Move `oxfmt` → devDependencies

## Phase 7: tooling and docs

- [x] ESLint boundary blocks (keep last, keep narrow): core bans `@tauri-apps/*` +
      node/react; server layer bans `@tauri-apps/*` except
      `src/server/adapters/**` + `runtime.ts`; plus `@tanstack/eslint-plugin-router`
      (`create-route-property-order`), perfectionist `unsorted` for Route objects,
      react-refresh `extraHOCs` for route creators
- [x] Vitest: frontmatter + format + fts-snippet tests (component tests removed
      with their components; a RouterProvider test wrapper returns when they do)
- [x] Rust unit tests: frontmatter parse, indexer reconcile
- [x] Rewrite README + AGENTS.md (stack, structure, patterns, commands, verification)

## Risks

Folded into [DECISIONS.md](DECISIONS.md), where each live risk sits with the decision that
accepts it as a constraint: frontmatter drift and exotic external frontmatter under `D6`,
watcher echo under `D16`, filename collisions and dangling wikilinks under `D5`. Two rows
described machinery this rewrite removed, so they carry no constraint now: CM6 live styling
was superseded by `D7`, and remark-stringify against oxfmt by `D15`.

## Verification

```
pnpm knip && pnpm typecheck && pnpm lint && pnpm test && pnpm build:web  # static gate
cargo test && cargo clippy  # in src-tauri
```

Manual walkthrough (`pnpm tauri dev`):

- [x] 1. First launch creates `~/notras/` + `.notras/index.db`; empty state shows
- [x] 2. ⌘N → type → autosave badge; the `.md` file exists on disk with the content
- [x] 3. Retitle the note → file renames on disk
- [x] 4. **The money shot:** `echo "# from claude" > ~/notras/agent-note.md` from a
      terminal (or have Claude Code write one) → note appears in the palette within ~1s
- [x] 5. ⌘K search a word → FTS hit with highlighted snippet
- [x] 6. Add `tags: [x]` + `pinned: true` via UI → frontmatter in the file updates;
      filter by tag in palette
- [x] 7. `[[` autocompletes another note; the pill navigates from the editor
- [x] 8. `/` shows the slash menu; inserts a task list
- [x] 9. Drag an image in → lands in `attachments/`, renders inline in the editor
- [x] 10. Edit a note in another editor while it's open (clean) → buffer reloads
- [x] 11. Quick-capture hotkey from another app → jot → esc → file in `inbox/`
- [x] 12. Delete `.notras/index.db`, relaunch → search still works (index rebuilt)
- [x] 13. `pnpm tauri build` → built artifact launches; "Open With" on a random `.md` works
- [x] 14. Type, then quit from the tray and again with ⌘Q → relaunch and the last
      keystrokes are on disk (nothing automated covers the quit handshake)

## Deferred

- iA parts-of-speech syntax highlighting + style check (needs real NLP, the wrong weight for this app)
- Content-block transclusion (`/file.md` embeds)
- Backlinks panel (wikilinks are one-directional this pass)
- Git integration UI (the folder is git-init-able by hand today)
- wdio + tauri-driver e2e (replaces the deleted Playwright smoke tests)
- Auto-update / code signing
- MCP server (only if agents ever need richer ops than file writes, such as search-as-a-tool)

## Progress log

| Date       | What landed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-16 | Serialized the tag writes in `useNoteTags`, from a review finding that held up. `NoteService.setTags` is a read-modify-write (`read` → `updateFrontmatter` → `write`) and all four call sites are fire-and-forget `void changeTags(...)`, so toggling faster than an IPC round trip put two writes in flight with no ordering guarantee and the older one could land last, leaving disk holding the stale set. Writes now chain through a promise queue, so the last call is the last write. The rollback was wrong in a plainer way: `previous` was captured per call, so an older request's failure restored a snapshot the user had already moved past even when the newer write had succeeded, and it toasted a failure they had superseded. Rollback is now guarded on being the newest request and falls back to the loader's tags, held in a ref because the write resolves long after the render that started it. Both surfaces got the fix at once, which is what extracting the hook in `D31` was for. **Not verified headless:** the ordering under fast toggling, which needs two writes genuinely overlapping                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-08-16 | Tag editing gained a ⌘K sub-view alongside the strip picker (`D31`), reversing a rejection I had made on bad grounds. A review finding proposed moving tag editing into the palette and I turned it down, leading with the argument that it contradicted `D28` and `D30`. That is not a reason: these entries record reasoning so it can be re-examined, and `D28` had been superseded by `D30` in the same sitting. On the merits the finding was right, and the evidence was already in the tree: `PaletteView` was a sub-view machine for `move` and `delete`, `CommandItem` shipped a `data-[checked=true]` checkmark nothing used, and pin had been a titlebar toggle _and_ a palette action since it shipped, so a note-identity control in both chrome and ⌘K was established rather than novel. Both surfaces now route through `useNoteTags`, lifted verbatim out of `note-tags.tsx` so the shipped surface's behaviour is unchanged; `DESIGN.md`'s two "no second way" rules were qualified to what they actually protect, since a second entry point over one implementation is what pin already is and a second copy of the logic is the real bug. `⌘⇧T` stays on the strip picker, mirroring `⌘D`. Found on screen while building it: per-row counts were misaligned, because the generated `CommandItem` appends its own `ml-auto` checkmark and a second `ml-auto` on the count split the free space with it, so a wider label pushed its count further right. Fixed in all three places carrying it, the tags view, the move view, and the `#` tag group, with a `flex-1` label and a fixed `tabular-nums` column. **Not verified headless:** the view staying open across toggles, and the counts lining up                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-16 | Labelled the add-tag button in both states, and fixed the overflow it exposed. The icon-only state was the one a note spends most of its life in, and a bare `TagPlus` at the end of a run of tag names said nothing, with no tooltip in the strip to fall back on. It now reads `add tag` always: icon and label agree, nothing shifts when the first tag lands, `aria-label` is gone because the visible text is the accessible name, and the two-state sizing that could have jittered the row is gone with it. Checking the extra width turned up a defect that `D30` had already written down as though it were true: the constraint claimed overflowing tags clip, but the chip container carried `min-w-0` with no `overflow-hidden`, and every chip is a `Button`, which is `shrink-0` in its base class. A container allowed to shrink whose children refuse to does not clip, it spills, so at narrow widths the chips ran over the word count. The chips now sit in their own `min-w-0 overflow-hidden` box with the button outside it, so the action survives at any width and only the display clips. The box is conditional rather than always rendered, since an empty flex item still takes the parent's `gap-1` and would have indented the button by 4px on an untagged note. **Not verified headless:** the strip at the 480px minimum, and that the row does not shift when the last tag goes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-16 | Made the strip's tags read as tags, from a screenshot. `D30` made a chip a filter button rather than a `ComboboxChip`, which is what kept the 28px strip flat, but it also removed the marking that said the words were tags: `saved  groceries  intro  #` put two bare names in a strip that otherwise carries status text, and the trailing `#` was the picker trigger yet read as one more tag. Chips now carry a literal `#`, and the trigger is `TagPlusIcon`, which cannot be mistaken for a name. The trigger carries the words `add tag` while the note has none and drops to icon-only once chips supply the context; `xs` and `icon-xs` are both `h-6`, so the row does not move between the two. `aria-label` is omitted in the labelled state so the accessible name is the visible text, and is `edit tags` in the icon state, which is honest because the popup removes as well as adds. Palette note rows got the same prefix through `tagLabel`, since `title · folder · work notes` had the identical problem. Picker rows keep their leading `HashIcon`. **Deferred:** the trigger is still the only button in the strip without a tooltip, which is also where `⌘⇧T` would surface; `Tooltip > TooltipTrigger render={ComboboxTrigger render={Button}}` is three-deep Base UI render composition and was not worth the risk in a change this size. **Not verified headless:** every visual claim above                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-08-16 | Tags are picked rather than typed (`D30`). Both halves of the tag UX had been hand-rolled over server capabilities that already existed and had no callers: `listTags()` returns every tag counted, grouped, and sorted by the index, and `getTagFilter()` matches exactly and ANDs with the FTS filter, so `getNotes({ tag, query })` was already tag-scoped full-text search. The editor instead was a `w-16` input with no suggestions, which let a typo fork `standup` from `standups` silently, and the palette recomputed the vocabulary with `notes.flatMap` on every keystroke and substring-matched it, disagreeing with the index it stood in for. The widget was one registry entry away: `components.json` is base-maia, whose `combobox` ships chips, empty, and item-indicator parts, and the installed `input-group.tsx` already carried `in-data-[slot=combobox-content]` hooks for a component nobody had added. Adding it regenerated `button`, `input`, `textarea`, and `input-group` byte-identically once `lint:fix` and `format:fix` ran, so the four had carried no customization. Tags moved to the status strip because the 44px titlebar could not hold them (`D28` is superseded in part), which also satisfies `D28`'s no-drag constraint by deletion rather than by widening the rule. `#` now parses through `parseTagQuery` in `src/lib/utils/tag-query.ts`, so `#work budget` searches inside the tag, the counted tag list shows as soon as `#` is typed, and actions stop vanishing in tag mode. Frontmatter is untouched: `core/frontmatter.ts`, `set-note-tags.ts`, `note-service.ts`, and both Rust files are byte-identical, so the index stays a derived cache (`D3`) and the files stay the source of truth (`D2`). One bug found on screen and fixed: `ComboboxContent` sizes to `--anchor-width`, and a 24px icon trigger collapsed the popup to the trigger's width. **Not verified headless:** the strip at the 480px minimum width, the popup opening upward, `⌘⇧T`, and a chip click scoping the palette                                                                                                                                                                                                                                                         |
| 2026-08-16 | Settled the titlebar geometry by copying [erictli/scratch](https://github.com/erictli/scratch) instead of deriving it. Four rounds went into this, each fixing the symptom in the screenshot and introducing another: buttons forced to `y: 12` in a 36px bar sat 7pt above the title and crowded the corner; removing the override for a 28px bar aligned them but left the bar cramped once it had a border; holding content in macOS's band with `pb-2` gave 7pt above the buttons against 16pt below. The churn came from deriving geometry off screenshots rather than reading a shipped implementation. scratch is the app `D2` and `D7` already credit, runs the same Tauri overlay titlebar, and pairs `trafficLightPosition: { x: 16, y: 24 }` with a 44px band; its shipped screenshot puts the buttons ~27px below the window top in that band, so centred. notras now uses the same pair, which also reverses round two: moving the buttons is the normal solution, and `D29`'s constraint had recorded my misreading as if it were a finding. tao's `y` is the button's centre offset from the window top, not a margin above it, and that explanation is kept in `D29` as the part worth not rediscovering. `Titlebar` drops to plain `items-center` with no padding, since centring in a band whose buttons are centred needs nothing else. scratch also sidesteps the alignment problem entirely by giving the buttons an empty spacer in its sidebar column while the editor toolbar sits in a different column at the same height; `D10` means notras has no sidebar, so only the height and offset transferred. **Not verified headless:** all of it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-08-16 | Titlebar correction, from a screenshot: the buttons sat 7pt above the title and crowded the rounded corner. Cause was mine, reading `trafficLightPosition`'s `y` as a top margin. tao's `inset_traffic_lights` resizes the titlebar container to `buttonHeight + y`, anchors it to the window top, and leaves each button's own offset inside it untouched, so in AppKit's bottom-up coordinates `y` lands as the button's centre offset from the window top. Setting 12 for a 36px bar centred the buttons at 12pt while the bar centred its text at 18pt. Fixed structurally rather than by correcting the number: the override is gone from both the config and the capture-window builder, and `Titlebar` is now `h-7`, macOS's own titlebar height, so the buttons and the content share a centre line by construction with nothing to keep in sync. The compact metrics themselves were fine all along, measuring ~10.5pt against Tahoe's 16pt default. Also gave the bar an edge: it had no border and no tone change, so the title read as stray text floating over the document rather than as chrome. It now mirrors the status strip's `border-t` with `border-b`, and both bands were brought to `h-7` so they frame the note symmetrically; the pin dropped to `size-6` to fit. Chrome is now 28 + 28 against the 36 + 40 + 32 it started at. **Not verified headless:** all of it, and specifically whether Tahoe's titlebar really is 28pt under compact metrics, which the whole approach rests on                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-16 | Compact window controls, and a titlebar that earns its height (`D28`, `D29`). The traffic lights were oversized because notras builds against macOS SDK 26.5, and Tahoe raised standard control metrics from 12x14 to 16x16 for anything linked against it. `use_compact_window_controls` sets `prefersCompactControlSizeMetrics` on both the `contentView` and the three standard window buttons, guarded by `objc2::available!(macos = 26.0)`; both are set because the buttons live in the frame view rather than under `contentView`, and which mechanism carries the property was not worth resolving for four lines. `objc2` and `objc2-app-kit` were already in the tree through tao and wry, so naming them directly added no packages. Rejected building against SDK 15.x, which fixes it globally but pins the toolchain and has to be reproduced on every machine that builds notras. Shrinking the buttons alone would have left more dead space, so the empty 36px drag strip absorbed the 40px note header: title, tags, and pin now sit in the titlebar, `trafficLightPosition` centres the buttons in it, and chrome drops from ~108px to ~68px with nothing lost. That moved the drag region out of `__root.tsx` into a shared `Titlebar` component, since a root-level strip cannot hold per-route content and TanStack Router has no named outlet; the empty state, the external-file route, and the capture window all render it now, which also settles the old h-9/h-8 inconsistency between the two windows. The title drops from `text-lg font-semibold` to `text-sm font-medium`, since 18px beside 12px buttons reads as a heading that wandered upward. **Not verified headless:** every visual and gesture claim above, and specifically whether macOS titlebar double-click-to-zoom collides with double-clicking the title input to select a word                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-16 | Killed the white launch flash (`D27`). Root cause was that notras declared `color-scheme` nowhere, so WKWebView painted its default white canvas until `styles.css` landed, and the CSS property could not help because honouring it waits on the stylesheet. `index.html` gains a `color-scheme` meta plus inline critical CSS for both schemes (the meta is read before the document is drawn, which is why it works); `styles.css` gains the real property in `@layer base`. Tauri's `backgroundColor` covers only the window layer on macOS, per its own "not implemented for the webview layer" note, so it is set in `tauri.conf.json` for the earliest frame and corrected from `window.theme()` in `setup()` so a light-mode launch does not flash dark instead; the capture-window builder gets the same treatment and a `WindowEvent::ThemeChanged` arm keeps it right when appearance changes at runtime. The capture window mattered most, since the first ⌘⇧N boots the whole bundle. Fixed a second bug with the same cause: without `color-scheme` the scrollbars, caret, and the code-block language `<select>` rendered light-scheme even in dark. `--background` now exists in four files, so `src/styles.spec.ts` grew 5 assertions tying every copy back to the token, verified to bite by drifting all three. Gate: 178 TS tests, 11 Rust tests, knip/tsc/eslint/oxfmt/build clean. **Not verified headless:** the flash itself, which needs `pnpm dev` in both appearances                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-16 | Aesthetic redesign onto the stet palette (`D23`-`D26`), taking the theme notras' siblings share: playa.dev already imports its colours from `stet/packages/tui/src/theme/{dark,light}.ts`, so notras adopting the same source puts three projects on one system. The stone oklch tokens are replaced by stet hex mapped onto shadcn's eleven role names, with seven values taken from playa.dev's lifted variants because stet treats contrast as a comfortable target. notras gains a real accent for the first time (`--primary` was near-white in dark and near-black in light), plus `--faint` and `--selection` as new roles and twelve named `--syntax-*` roles replacing the five-colour chart ramp, which let the six `.hljs-*` groups grow to fourteen with italic comments and bold keywords. Type drops from four faces to three: Literata replaces iA Writer Quattro on the note surface, since iA's duospace is designed to slow reading and notras is read as much as written; `system-ui` replaces DM Sans so chrome matches the host; iA Writer Mono absorbs Geist Mono and `--font-editor-mono`. The wordmark is the only chrome in the mono. No file in `src/components/ui/**` was touched: reconfiguring shadcn is a variable edit, so `D19` and `D22` stand. Radii and the blur/zoom dialog motion were kept deliberately as macOS convention, and `D26` adds the concentric rule where notras owns the CSS. Found and fixed on the way: `@tailwindcss/typography` was rendering the note in its own stone ramp, so the app's most important surface was off-palette entirely; `.note-preview-prose` now repoints all sixteen `--tw-prose-*` colours and `prose-stone dark:prose-invert` came off the editor. Also folded in a `prefers-reduced-motion` handler (neither reference ships without one) and replaced the ad-hoc `text-muted-foreground/60` and `/50` opacities with `--faint`. New gate: `src/styles.spec.ts` (78 tests) measures every text-on-surface pair against WCAG AA in both schemes and asserts the schemes declare identical token names, verified to bite by reverting the light card. **Known gap:** command-palette items sit 4px off concentric, since the value lives in `ui/command.tsx` and the radius ladder's 4px and 8px steps cannot express it (`D26`) |
| 2026-08-16 | Radix UI → Base UI (`D22`): `components.json` moves to the base-maia style and the 11 surviving `ui/` components regenerate against `@base-ui/react@1.7.0`, dropping `radix-ui` and the 57 packages behind it. The 10 components with no consumer were deleted rather than ported (`alert-dialog`, `alert`, `avatar`, `card`, `dropdown-menu`, `field`, `select`, `separator`, `sheet`, `skeleton`), which also removed every `--radix-*` variable and `data-[state=…]` selector outside `tooltip.tsx`. `asChild` becomes the `render` prop at the two `TooltipTrigger` sites; `Slot.Root` in `button.tsx` and `badge.tsx` becomes `@base-ui/react/button` and `useRender`. The `command.tsx` a11y deviation was re-applied after regeneration, `#root` gained `isolation: isolate` for portal stacking, and the `**/components/ui/**` lint override shrank from 12 rules to the 5 that still fire. Review pass on top: the generated close labels in `dialog.tsx` and the default palette title and description in `command.tsx` were lowercased for `D18` and recorded as a second `D19` deviation, and four stale claims were corrected in this file (the `D1`-`D22` range, `index_paths` as a TypeScript write path against `D4`, preferences reaching TypeScript through the `FileStore` port rather than a direct plugin-store import, and the remark/react-markdown/codemirror entries that `D7` and `D15` superseded). Two stale `components.json` fields fixed on the way through: `rsc: true` (`D1` is a Vite SPA) and a `tailwind.css` path pointing at a file deleted in Phase 6. `cmdk` and `sonner` were left alone                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-16 | Docs reorganized by extraction, following the structure in a sibling repo's `AGENTS.md`. `AGENTS.md` becomes rules plus a doc map and holds no project fact; `ARCHITECTURE.md` takes the stack, structure, layer boundaries, key patterns, index schema, and a new Invariants section; `DESIGN.md` is new and holds the interface conventions, grounded in `src/styles.css`; `DECISIONS.md` is new and backfills `D1` through `D21` from this spec's locked table, the Cut list, and the Risks table. `AGENTS.md` gains code-craft rules (naming, unit shape, boundaries, editing, types, testing, bug fixing), a stop-and-ask protocol, an evidence-and-verdicts section, and a prose standard that bans em dashes and marker punctuation across every markdown file. The `V`/`T`/`B` ID system was considered and not adopted. Two stale claims found while moving: `src/lib/preferences.ts` and `@tauri-apps/plugin-store` on the TS side no longer exist, so preferences reach TS through the `FileStore` port, and the structure tree was corrected to the real `src/lib/` contents                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-16 | Effect 3 → 4 (`4.0.0-rc.109`): 117 type errors → 0, all cascading from 9 files. `Context.Tag` → `Context.Service` classes carrying `static readonly layer` (the `XxxLive` consts are gone); `Tag.pipe(Effect.flatMap(...))` → `Service.use(...)` across all 15 `src/data/` files, which also retires the `unicorn/no-array-method-this-argument` workaround; `Data.TaggedError` → `Schema.TaggedError` (`Schema.Defect()` for `cause`); `Cause.failureOption` → `findErrorOption`; `ManagedRuntime.Context` → `Services`; Schema filters → `Schema.check(isMinLength/isMaxLength/isPattern/makeFilter)` with string messages; `Logger.pretty` → `Logger.layer([Logger.consolePretty()])`, now merged rather than provided so it actually applies. Repository's 6 duplicated `tryPromise` blocks collapsed into one `dbQuery`; `Effect.fn` on generator bodies. First tests over the Effect layer: `note-schemas.spec.ts` pins the user-facing validation strings, `note-service.spec.ts` uses the long-unused DI seam via a new `NoteService.layerNoDeps`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-08-15 | Manual walkthrough passed end to end (all 14 steps), confirming the two things headless checks could not reach: images render through the asset protocol under the new CSP, and the runtime scope grant survives a notes-folder switch. Second review pass on top: fixed a Shift-Tab that outdented line 1 from anywhere in source mode, closed a scheme-gate hole (the platform `URL` parser now reads schemes, so `java&Tab;script:` cannot pass as schemeless), stopped the nbsp scrub from rewriting `&nbsp;` inside code spans and fences, made a failed flush cancel the quit instead of exiting anyway (`cancel_quit` + a conditional Rust backstop), rejected symlinks in `index_file`, surfaced dropped "Open With" paths, and guarded quick-capture against a double save                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-08-15 | Code review pass: `db_select` now gated on `sqlite3_stmt_readonly` (writable CTEs were slipping through the `WITH` prefix test); real CSP + runtime-managed asset scope replacing `csp: null` / `$HOME/**`; autosave serialized on one chain and awaited on quit (Rust holds `ExitRequested`); frontmatter parity fixes across both parsers (value-wide dedupe, separator-stripping tags, CRLF raw lines, `---` delimiters with trailing whitespace); link scheme allowlist; wikilink HTML round-trip; note header follows the loader; plus ~20 smaller correctness/a11y fixes. SPEC's editor sections rewritten for TipTap (they still described CodeMirror)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-15 | Editor polish: code blocks get hover chrome (copy button + language picker via React nodeview); links become functional (⌘⇧K add/edit/remove popover with url normalization, ⌘-click opens in browser via plugin-opener)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-15 | Source mode fidelity saga: highlighted source mode (single lowlight code block) with anchored toggles; block-offset heuristics replaced by exact sentinel round-trip through the markdown converters (caret spot survives toggles deterministically; corruption guard for syntax-interior carets); suggestion menus viewport-clamped                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-08-15 | Studied erictli/scratch's shipped editor and adopted its polish: task-list CSS recipe (flex rows, zero p margins, hand-drawn checkboxes, which fixed broken task rendering), nbsp scrub on every serialize, markdown-sniffing paste, copy-as-markdown, [text](url) input rule, clipboard image paste via new attach_image Rust command (improved over scratch: relative attachments/ src stays in the file), tables not-prose, autocorrect/autocapitalize off (lowercase aesthetic + kills the blue underline), wikilinks restyled to dashed underline. Skipped: KaTeX/mermaid, code-block nodeview, frontmatter-as-node (ours is race-proof)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-15 | Editor pivot: CM6 live preview replaced by TipTap 3 WYSIWYG (official @tiptap/markdown made round-tripping viable); wikilink pills with custom tokenizer, slash menu + wikilink suggestions, editable tables, clickable tasks, inline images, lowlight code; body-only editing with frontmatter composed from latest loader state; ⌘P becomes raw source mode; format-on-blur + remark stack + react-markdown preview + syntaxHighlighting pref removed; 14-case markdown round-trip spec is the new data-safety contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-08-15 | Live preview phase 2: blocks + widgets (bullets, clickable checkboxes, code cards with hidden fences, hr, inline images, rendered tables); ViewPlugin → StateField for block decorations; fixed markdown base to GFM (`markdownLanguage`), since tables/tasks/strikethrough were not parsing at all before                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-08-15 | Live preview: hand-rolled CM6 mark-hiding extension (research ruled out ixora/rich-markdoc as dead, codemirror-live-markdown as alpha); ⌘P kept as reading mode with editor-matched type scale; overlay-transition WIP reverted as wrong direction                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-15 | Spec v1 (SQLite-first, keep-everything)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-08-15 | README + AGENTS.md rewritten for the files-first desktop app; rewrite landed as five conventional commits on feat-tauri-rewrite                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-15 | Phases 0-6 built and green: Rust core (7 tests), files-first data layer, CM6 editor (focus/typewriter/wikilinks/slash), ⌘K palette, quick capture, tray; full static gate passes (format/knip/lint/tsc/vitest/build); live smoke: external write indexed <2s, delete purged, quick capture verified in-app. TanStack ESLint plugin adopted after review. Remaining: README/AGENTS rewrite, shift+/ help, tauri build bundle check                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-15 | Spec v2: pivot to files-first after scratch review; editor-first layout, CM6 editor, autosave; cut links, reminders, export/import, assets-as-blobs, profile; adopted wikilinks, slash commands, open-external, quick capture, tray, overlay chrome, autostart                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
